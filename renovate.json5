{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  extends: [
    // Filename is explicit: bare references only resolve default.json.
    "local>adampie/renovate-config:default.json5",
    "schedule:weekly",
  ],
}
